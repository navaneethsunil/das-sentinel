# DAS Sentinel web image — STUB (M0-I2).
# Real build (Next.js install/build/start) lands in M0-F1.
# Pinned base per CLAUDE.md §3 (Node 24 LTS); digest-pin as a follow-up (TR-25).
FROM node:24-slim

WORKDIR /app

USER node

# Placeholder: keep the container alive so infra bring-up can be validated this chunk.
# M0-F1 replaces this with the Next.js app.
CMD ["sleep", "infinity"]
